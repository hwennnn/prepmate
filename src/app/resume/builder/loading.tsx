import { LoadingSpinner } from "~/components/ui/loading-spinner";

export default function ResumeBuilderLoading() {
  return (
    <LoadingSpinner fullScreen text="Loading resume builder..." size="lg" />
  );
}
